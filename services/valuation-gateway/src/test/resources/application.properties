# Flyway owns the schema; Hibernate only validates (there are no JPA entities in the gateway, so nothing to validate).
spring.flyway.locations=classpath:db/migration/gateway
spring.flyway.schemas=gateway
spring.jpa.hibernate.ddl-auto=validate
