# Flyway owns the schema; Hibernate only validates that entities match the migrations.
# WHY validate: a mismatch between entity and migration fails the test instead of surfacing in production.
spring.flyway.locations=classpath:db/migration/pricing
spring.flyway.schemas=pricing
spring.jpa.hibernate.ddl-auto=validate
